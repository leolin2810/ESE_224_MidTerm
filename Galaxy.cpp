#include "Galaxy.h"
#include <iostream>
#include "Probe.h"

void Galaxy::addProbe()
{
    std::string n;
    int id, length, width, x, y; 
    std::cin >> n >> id >> length >> width >> x >> y;
    Probe p;
    
}